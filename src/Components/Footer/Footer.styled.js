import styled from "styled-components";
import { headingColor } from "../Reusable/Colors";

export const FooterParentDiv = styled.div`
  background-color: rgb(27 30 37);
  padding: 15px 0;

  p {
    color: ${headingColor};
    text-align: center;
  }
`;
